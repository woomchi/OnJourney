/**
 * @fileoverview 요금 관련 공통 상수
 */

/** Fallback 택시 기본 요금 (원) */
export const TAXI_BASE_FARE = 4_800;

/** Fallback 택시 거리 요금 계수 (원/km) */
export const TAXI_DISTANCE_RATE = 1_100;

/** Fallback 택시 할증 계수 */
export const TAXI_SURCHARGE_FACTOR = 1.3;
