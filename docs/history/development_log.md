# OnJourney 개발 흐름 로그

> Git 커밋 이력을 기반으로 정리한 프로젝트 개발 흐름 요약입니다.  
> 세부 변경 사항보다는 **어떤 기능이 어떤 순서로 추가·개선·제거되었는지**의 흐름에 집중합니다.

---

## Phase 1 — 기초 기능 구축 (2026-06-14 ~ 06-16)

### 장소 추가 & 인증
- 장소 추가 기능 초기 구현 및 반복 수정
- 로그인 관련 기능 개선

### 지도 핵심 기능
- 이동 수단 **타임라인 바** 디자인 구조 구현
- **대중교통 정보 연동 및 Polyline** 렌더링 구현
- **줌 아웃** 기능 구현
- Polyline **방향 마크 & 구간 하이라이트** 기능 추가
- 장소 순서 변경 시 경로 자동 업데이트 로직 적용
- 서버 시작 시 무한 로딩 오류 수정

---

## Phase 2 — 지도 시각화 & 마커 정교화 (2026-06-17 ~ 06-18)

### Polyline 시안성 개선 (4단계에 걸친 반복 개선)
1. 1차 — Polyline 기본 시각 개선
2. 2차 — 마커 시안성 동시 개선
3. 3차 — 대안 교통수단 선택 UI 병행 구현
4. 4차 — **Polyline 레이어 분리 및 우선순위 부여**

### 마커 시스템
- **탑승 마커** 구현 (택시 요금 오류 보정 포함)
- 탑승 마커 클릭 시 세부 노선 줌 인 & 하이라이트
- **도보 마커** 추가 및 다음 이동 정보 마커 연동
- 장소 마커 표시 기준 인터랙션 정비

### 자동차 경로
- **자동차 경로** 기능 구현 및 마커 로직 수정

### UI
- 타임라인 바 하단 대중교통명 배치
- 전체 경로 표시 버튼 디자인 수정
- 사이드바 너비 조정, 아코디언 인터랙션 수정

---

## Phase 3 — 상세 패널 & 실시간 정보 시스템 (2026-06-19 ~ 06-21)

### 상세 정보 패널 신설
- **차량 및 대중교통 상세 정보 패널** 구현
- 상세 패널 내 구간 카드 인터랙션 구현
- 타임라인 바 하이라이트 방식 수정
- 특정 세부 구간 선택 시 **자동 스크롤** 기능 추가
- 이동 정보 패널에서 다음 패널로 이동하는 버튼 UI 추가
- 상세 패널 위치 정렬 및 최종 목적지 카드 디자인 수정

### 여정 편집 기능
- 여정 정보(제목/날짜/이동수단) **상시 편집 방식**으로 전환
- 날짜 지정 제한 해제
- 차량 경로 대안 세분화

### 줌 & 뷰 조정
- 가상 박스 기반 줌 배율 세분화
- 패널 추가에 따른 줌 방식 동적 조정
- 검색바 삭제 및 이에 따른 줌 조절 방식 수정

### 실시간 정보 시스템 (추가 후 임시 제거)
- **버스 및 지하철 실시간 정보** 시스템 구축
- 대중교통 정보 일괄 호출 방식 수정
- 가짜 실시간 데이터 로직 제거 → 새로고침 애니메이션 추가
- 사이드바 내 실시간 정보 UI 임시 삭제

### 여정 컨트롤
- 여정 컨트롤(재생/정지 등) 기능 구현
- 줌 인아웃 인터랙션 세부 수정

---

## Phase 4 — 구조 정리 & 성능 최적화 (2026-06-23 ~ 06-24)

### 대규모 리팩토링
- 핵심 프론트 파일, 사이드바, 상세 패널 코드 리팩토링
- **파일 분리 및 프로젝트 구조 정리**
- **shadcn/ui 도입** — UX/UI 디자인 요소 체계화
- 코드 분할 및 렌더링 최적화

### 데이터 패칭
- **React Query 도입** — 관련 로직 오류 및 버그 수정
- 가짜 대중교통 데이터 생성 로직 완전 제거

### 대안 패널
- 대안 아코디언 분리 → **독립 패널**로 전환
- 대안 패널 디자인 수정 및 경로 노출 기준 변경
- 대안 카드 디자인 개선

### 경로 렌더링
- **경로 렌더링 애니메이션** 추가
- 순서 기반 경로 색상 버그 수정
- 도보 구간 점선 색상 부여

### 재생 기능
- 여정 재생바 인터랙션 구현
- 사이드바 재생 버튼 구조 개편

---

## Phase 5 — 사이드바 고도화 & 검색 기능 (2026-06-25 ~ 07-05)

### 사이드바 UI & 인터랙션
- 마커 색상 순서 기반 테마 적용
- 사이드바 색상 불일치 버그 수정
- 드래그 앤 드롭 **햅틱/시각 효과** 구현
- 상세 패널 & 대안 패널 **생성 애니메이션** 추가
- 장소 순서 UI 효과 및 재생 타임라인 바 **물결 효과** 추가
- 재생 버튼 인터랙션 추가
- 편집 모드 활성화에 따른 패널 **자동 닫힘** 기능 추가
- 편집 모드 활성화 시 **자동 줌 조정**

### 검색 기능
- 사이드바 검색 모드 전환 및 **추천 기능** 구현
- 검색 로직 반복 수정 (총 3회)
- 검색 하이라이트 방식 변경
- 현재 화면 기준 검색 버튼 배치 및 인터랙션 수정
- GPS 응답 속도 개선

### 성능 & 안정성
- 캐싱 최적화 및 리렌더링 문제 해결
- 마커 및 Polyline 렌더링 지연 문제 해결
- 지도 렌더링 및 상태 동기화 최적화
- 사이드바 검색 화면 로직 수정 및 애니메이션 추가

---

## Phase 6 — 모바일 환경 대응 (2026-07-06 ~ 07-20)

### 모바일 구조화 & UI
- 초기 **로그인 화면** 구현 및 모바일 구조 체계화
- 모바일 UI/UX 1차 작업
- 모바일 UI 시안성 향상

### 계정 인터랙션
- 계정 관련 인터랙션(프로필, 세션 등) 추가

### 바텀 시트 시스템
- **상세/대안 패널 → 바텀 시트** 방식으로 전환
- 바텀 시트 드래그 애니메이션 개선
- 높이 조절에 따른 내부 요소 인터랙션 수정 (여정/장소/이동 카드, 줌 조정)
- 상세 패널 UI → **플로팅 플레이어 바** 방식으로 변경
- 여정 상세 바텀 시트 스크롤 감도 및 여백 수정
- 상세 바텀 시트 세부 이동 정보 **UI 개편 및 스크롤 방식** 수정

### GPS & 센서
- **모바일 GPS 기능** 구현
- **자이로스코프(방향 감지)** 기능 구현
- GPS 기능 보류 → 이후 복구 작업

### 시스템 최적화
- 모바일 환경 시스템 전반 최적화
- 검색바 위치 조정 및 **오버 스크롤** 기능 추가
- 지도 줌 버그 수정

---

## Phase 7 — 캐싱 전략 고도화 (2026-07-28)

### 시간대별 캐싱 시스템
- **시간대별 캐시 키 분리** 구현
  - 평일/주말, 낮/밤 시간대별 캐시 분리 (4가지 시간대)
  - 밤 시간대 올바른 대중교통 경로 제공 문제 해결
  - `getTimeSlot()` 함수로 시간대 분류

### 출발 시간 기반 캐싱
- **출발 시간 파라미터 추가** 구현
  - `departureTime` 파라미터 추가 (선택적, Unix timestamp)
  - 3시간 단위 시간 그룹화로 캐시 파편화 최소화
  - 예약 여정 계획 기능 지원
  - 클라이언트/서버/스키마 전체 수정

### 동적 캐시 만료 시간
- **시간대별 동적 캐시 만료 시간** 구현
  - 밤 시간대 (23:00-06:00): 30분 캐싱 - 데이터 신선도 확보
  - 낮 시간대 (06:00-23:00): 4시간 캐싱 - API 절감 극대화
  - 차량 경로에 동적 캐시 기간 적용
  - ODsay API 무료 플랜 제한(1,000회/일) 고려 최적화

### 성능 최적화 효과
- 밤 시간대 데이터 신선도 확보
- 낮 시간대 API 호출 75% 감소
- 시간대별 올바른 경로 정보 제공
- 예약 여정 계획 기능 지원

---

## Phase 8 — 인프라 고도화 & 신규 서비스 연동 (2026-07-29 ~ 2026-08-12)

### 장소 검색 API 전환
- **네이버 장소 검색 → 카카오 로컬 API** 전환
  - 키워드 검색 + 카테고리 검색 이중 구조
  - `searchPatternService.ts` 도입: 검색어 패턴 분석 + 카테고리 코드 자동 매핑
  - **Gaussian Decay 거리 점수** 기반 랭킹 (도보/도심/외곽 scale 구분)
  - Cold Start 방지용 Tier1 카테고리 인기도 가산점

### 도보 경로 실제 API 연동
- **ODsay 멀티모달 API 연동** (`odsayWalkingService.ts`)
  - 10km 미만 구간에 ODsay `maasRP` (멀티모달 API) 기반 도보/자전거/킥보드 경로 탐색 적용 (기존 TMAP 보행자 API 대체)
  - 단일 `ODSAY_API_KEY` 사용으로 API 인증 관리 통합 및 데이터 일관성 확보
  - ODsay API 장애 또는 범위 초과 시 `buildWalkFallbackResults` 기반 Fallback 자동 전환

### 인프라 패턴 도입
- **Circuit Breaker** 패턴 구현 (`circuitBreaker.ts`)
  - CLOSED → OPEN → HALF_OPEN 3상태 관리
  - ODsay 연속 실패 3회 시 즉시 Fail-Fast Fallback
  - 10초 쿨다운 후 HALF_OPEN 복구 시험
- **Rate Limiter** 구현 (`rateLimiter.ts`): ODsay 무료 플랜(1,000회/일) 보호
- **OdsayAdapter** 도메인 에러 클래스 체계화
  - `TransitApiError`, `TransitAuthError`, `TransitQuotaError`, `TransitRouteNotFoundError`

### 지형 분류 시스템
- **GeoJSON 기반 지형 분류** 구현 (`walkabilityCheck.ts`, `terrainClassifier.ts`)
  - Phase 1: O(1) BBox 선검사 → Turf.js PIP 2차 정밀 검증
  - `naturalData.json` + `landUseData_processed.json` 파일 로드 (forest, wood, water, beach 등)
  - 지형 타입 분류: `normal` / `mountain` / `beach`

### DnD 라이브러리 전환
- **HTML5 Native DnD → `@dnd-kit`** 전환 (core / sortable / modifiers)
  - 모바일 터치 지원 향상
  - `useMobileDndSensors` 훅으로 센서 설정 추상화

### 출발 시간 선택 UI
- `DepartureTimePicker` 및 `DepartureTimeSelector` 컴포넌트 추가
  - 여정 출발 시간 직접 지정 기능
  - 기존 `departureTime` 파라미터 캐시와 연동

### GPS 트래킹 복구
- Phase 6에서 보류된 GPS 기능 **`useGPSTracking` 훅**으로 재구현
  - `useGeocodeOnIdle.ts`: 지도 idle 상태 기반 역지오코딩

### 피처 모듈화 (features/ 디렉터리)
- `features/map/`: MapArea, MapMarkers, MapRoutes, MapOverlays, MapFloatingControls, useMapCamera
- `features/route/`: AlternativeRoutePanel, RouteGuidePanel, RoutePanels
- `features/places/`: PlaceSearchBar, usePlaceSearch

### React Query 영속화
- `@tanstack/react-query-persist-client` + `idb-keyval` 도입
  - IndexedDB 기반 쿼리 캐시 영속화 (앱 재시작 시 캐시 유지)

---

## 삭제 / 임시 제거된 기능

| 항목 | 시점 | 비고 |
|------|------|------|
| 가짜 대중교통 데이터 생성 로직 | Phase 4 | 실 API 연동으로 대체 |
| 가짜 실시간 데이터 로직 | Phase 3 | 실 API 연동 전 임시 제거 |
| 사이드바 내 실시간 정보 UI | Phase 3 | 임시 삭제 (재설계 예정) |
| 검색바 | Phase 3 | 줌/뷰 방식 개편으로 제거 |
| GPS 기능 (일시 보류) | Phase 6 | Phase 8에서 `useGPSTracking`으로 복구 |
| 마커 애니메이션 | Phase 5 | 성능/디자인 이유로 삭제 |
| 네이버 장소 검색 API | Phase 8 | 카카오 로컬 API로 전환 |

---

## 주요 기술 도입 이력

| 기술 / 도구 | 도입 시점 | 목적 |
|------------|----------|------|
| `shadcn/ui` | Phase 4 (06-24) | UI 컴포넌트 체계화 |
| `React Query` | Phase 4 (06-23) | 서버 상태 관리 최적화 |
| 바텀 시트 패턴 (`framer-motion`) | Phase 6 | 모바일 UX 최적화 |
| Polyline 레이어 분리 | Phase 2 | 렌더링 우선순위 제어 |
| 시간대별 캐시 키 분리 | Phase 7 | ODsay API 비용 절감 |
| 출발 시간 파라미터 (`departureTime`) | Phase 7 | 예약 여정 계획 지원 |
| Circuit Breaker | Phase 8 | ODsay 장애 보호 및 Fail-Fast |
| Rate Limiter | Phase 8 | ODsay 무료 플랜 쿼터 보호 |
| TMAP 보행자 경로 API | Phase 8 | 실제 도보 경로 탐색 |
| 카카오 로컬 API | Phase 8 | 장소 검색 개편 (네이버 대체) |
| `@dnd-kit` | Phase 8 | DnD 모바일 지원 향상 |
| GeoJSON + Turf BBox+PIP | Phase 8 | 지형 분류 시스템 |
| `@tanstack/react-query-persist-client` | Phase 8 | 쿼리 캐시 IndexedDB 영속화 |

---

*최종 업데이트: 2026-08-12*
